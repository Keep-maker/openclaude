# 验证记录与交付边界

验证日期：2026-09-06。源码基线：`cb06ba81e400f11f404d978e621792543ead758a`。本交付未推送 GitHub，修改保存在交付源码中。

## 1. 执行环境

- 按用户要求使用云端 rsandbox，未在本机安装 Node/Compose 或运行构建、测试、模型推理。
- 云服务器实际为 Debian 12、Docker Engine 20.10.24，Compose 独立命令 v2.29.7。
- 服务器缺少约定的 `rsandbox-base`，本次用已有 Ubuntu 22.04 镜像补齐标准工具底层，再基于它添加 Node 层；没有调整现有镜像加速器。
- 测试容器：`rsb-openclaude-20260906`，使用 rsandbox 辅助脚本创建，限制 768 MiB 内存、1 CPU。
- Node.js：22.23.2；应用基础镜像摘要：`sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`。
- Compose 测试项目：`rsb-oc-check`；仅临时发布服务器 `127.0.0.1:21436`，没有开放公网端口。
- 使用模拟上游及明确的测试占位字符串，没有复制用户凭据、API Key、OAuth 或 SSH 密钥进容器。

## 2. 已通过的验证

| 项目 | 实际结果 |
|---|---|
| `npm test` | **80 项通过，0 失败、0 跳过**；约 9.7 秒 |
| 新增前台监听测试 | 默认回环、显式 `0.0.0.0`、随机端口探活、配置目录含空格、SIGTERM 正常退出 |
| 原有回归 | 路由、鉴权、provider 隔离、OpenAI 协议转换、SSE、工具调用、超时与错误、模型选择等 |
| `docker-compose config --quiet` | 默认文件通过 |
| `--profile ollama config --quiet` | profile 合并与服务配置通过；未拉取 Ollama 镜像/模型 |
| `-f docker-compose.yml -f docker-compose.dev.yml config --quiet` | 开发覆盖文件通过 |
| Docker 镜像构建 | 单阶段构建成功；没有安装 npm 依赖 |
| 实际 Compose 启动 | router 启动成功，健康状态 `healthy` |
| 宿主机映射端口 | `127.0.0.1:21436` 返回 `ok:true` |
| 容器权限 | `uid=1000(node)`；只读根文件系统；尝试写挂载配置被拒绝 |
| 目录配置热读取 | 向只读挂载的宿主机目录更新模拟 provider，无需重启即被发现 |
| Compose 网络 | router 能通过容器名称访问同网络中的模拟 OpenAI-compatible 服务 |
| 真实镜像内非流式转换 | 模拟 OpenAI JSON 转为 Anthropic Message，中文内容与停止原因正确 |
| 真实镜像内流式转换 | 中文内容与 `message_stop` 正确，无流内错误事件 |
| 真实镜像内鉴权隔离 | 上游只收到模拟 provider Key，未收到客户端测试 Bearer |
| 真实镜像内 token 估算 | `/v1/messages/count_tokens` 返回有效估算值 |
| 正常停止 | 生产与开发容器退出码均为 0，未发生 OOM |
| 开发 watch | 修改挂载源码后出现重启日志，随后 HTTP 探活成功 |
| 差异检查 | `git diff --check` 通过 |

完整回归结果摘要：

```text
测试总数：80
通过：80
失败：0
取消：0
跳过：0
耗时：9665 毫秒
```

新增测试在 `test/router-listen.test.js`，已纳入默认 `npm test`，也可单独使用 `npm run test:deployment`。现有 HTTP/SSE 测试均使用模拟上游，可以重复执行而无需真实模型账户。

## 3. 未验证的范围

- 未执行真实 Claude Code 登录、订阅推理、模型选择器 UI 或实际子 Agent 任务。
- 未使用真实 Agnes、Ollama Cloud、Anthropic Key；未证明示例 API 地址、模型权限或额度当前可用。
- 未下载/运行 Ollama 模型，也未验证 GPU 或大模型吞吐；Ollama profile 仅验证 Compose 配置有效性。
- 未在 Windows/macOS Docker Desktop 实机测试；Windows 入口路径修复依据标准 `pathToFileURL`，不能视为 Windows 实测。
- 未做公网反向代理、TLS、多租户、容量或长时间负载测试；默认部署用于可信客户端。
- 新的 10 秒关闭宽限期验证了正常退出，未专门测量持续超过该期限的真实上游长流行为。

## 4. 交付和维护

完整源码包含原项目与本次修改，中文阅读顺序为：

1. [Agent 配置与使用](01-Agent配置与使用.md)
2. [Docker Compose 部署及文件原文](02-Docker-Compose部署.md)
3. 本验证记录

默认部署只运行 router。选择实际 provider 并提供自己的 Key 后，需在目标环境完成一次真实模型请求和工具调用往返，才能确认该后端满足你的 Agent 工作流。应用不自动刷新显式 OAuth，也不自动实现入口鉴权。
